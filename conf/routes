# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# An example controller showing a sample home page
GET     /                           controllers.HomeController.index()
GET     /hello                      controllers.HomeController.hello(name: String)
GET     /show                       controllers.HomeController.show(id: String)
DELETE  /delete                     controllers.HomeController.remove(id: String)
GET    /create                     controllers.HomeController.create()
+ nocsrf
POST    /save                     controllers.HomeController.save()
+ nocsrf
POST     /update                     controllers.HomeController.update(id: String)
GET     /list                       controllers.HomeController.list()
GET     /edit                       controllers.HomeController.edit(id: String)
# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.versioned(path="/public", file: Asset)

