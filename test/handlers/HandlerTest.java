package handlers;

import static junit.framework.TestCase.assertEquals;
import static org.mockito.Mockito.mock;

import com.typesafe.config.Config;
import java.util.concurrent.ExecutionException;
import org.junit.Test;

public class HandlerTest {


  // TODO: 13.03.19 add real tests
  @Test
  public void getPageCount() throws ExecutionException, InterruptedException {
    /*Config config = mock(Config.class);
    Handler mock = mock(Handler.class);
    Integer pageCount = mock.getPageCount().toCompletableFuture().get();
    assertEquals(5, pageCount.intValue());*/
  }
}